apiVersion: v1
kind: Service
metadata:
    name: sattwikroyaccountsmicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sattwikroyaccountsmicroservice